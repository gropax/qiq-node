{
  "name": "qiq",
  "version": "0.0.0",
  "description": "Web app for note management",
  "license": "ISC",
  "main": "app.js",

  "directories": {
    "test": "tests"
  },

  "scripts": {
    "start": "concurrently \"npm run tsc:w\" \"npm run lite\" ",    
    "tsc": "tsc",
    "tsc:w": "tsc -w",
    "lite": "lite-server",
    "typings": "typings",
    "postinstall": "npm run typings install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "dependencies": {
    "angular2": "2.0.0-beta.11",
    "systemjs": "0.19.24",
    "es6-promise": "^3.0.2",
    "es6-shim": "^0.35.0",
    "reflect-metadata": "0.1.2",
    "rxjs": "5.0.0-beta.2",
    "zone.js": "0.6.4",
    "typings":"^0.7.9"
  },

  "devDependencies": {
    "concurrently": "^2.0.0",
    "lite-server": "^2.1.0",
    "typescript": "^1.8.9"
  }
}
